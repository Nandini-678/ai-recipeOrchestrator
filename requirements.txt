# What the deployed app needs to serve a request, and nothing more.
#
# Chroma is deliberately absent: the vector index is optional (see
# requirements-dev.txt). It is never imported when serving, and it would add
# ~175MB of onnxruntime and friends to a build that gains nothing from them --
# at this corpus size, scoring every recipe takes 40ms.

groq==1.7.0             # LLM calls (composer, substitution fallback)
requests==2.34.2        # TheMealDB + USDA FoodData Central HTTP calls
python-dotenv==1.2.3    # load .env in local development
pydantic==2.13.5        # schema validation for all LLM output
streamlit==1.63.0       # UI
